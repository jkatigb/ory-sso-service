<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SSO Test Client</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
  <style>
    .container {
      max-width: 800px;
      margin-top: 50px;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">SSO Test Client</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link active" href="/">Home</a>
          </li>
          <% if (user) { %>
            <li class="nav-item">
              <a class="nav-link" href="/profile">Profile</a>
            </li>
          <% } %>
        </ul>
        <ul class="navbar-nav">
          <% if (user) { %>
            <li class="nav-item">
              <a class="nav-link" href="/logout">Logout</a>
            </li>
          <% } else { %>
            <li class="nav-item">
              <a class="nav-link" href="/login">Login</a>
            </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="card">
      <div class="card-body">
        <h1 class="card-title">Welcome to the SSO Test Client</h1>
        
        <% if (user) { %>
          <div class="alert alert-success mt-4">
            <h4>You are logged in!</h4>
            <p>Welcome, <%= user.name || user.email || 'User' %></p>
          </div>
          
          <div class="mt-4">
            <a href="/profile" class="btn btn-primary">View Profile</a>
            <a href="/logout" class="btn btn-secondary">Logout</a>
          </div>
        <% } else { %>
          <p class="card-text">This is a simple test application to demonstrate SSO integration with our Identity Provider.</p>
          <a href="/login" class="btn btn-primary">Login</a>
        <% } %>
      </div>
    </div>

    <div class="card mt-4">
      <div class="card-header">
        About This Demo
      </div>
      <div class="card-body">
        <p>This application demonstrates how to integrate with our SSO service using:</p>
        <ul>
          <li>OpenID Connect protocol</li>
          <li>Authorization Code Flow</li>
          <li>User profile information retrieval</li>
          <li>Proper session management</li>
        </ul>
        <p class="mt-3">
          The source code for this application is available in the 
          <a href="https://github.com/jkatigb/ory-sso-service/tree/main/examples/nodejs-test-client" target="_blank">GitHub repository</a>.
        </p>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>